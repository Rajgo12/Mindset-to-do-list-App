name: appdev_project
description: "A new Flutter project."
publish_to: 'none' # OK for private project

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  cupertino_icons: ^1.0.8
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # latest as of now (fixes your indentation issue too)

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/signup.png
    - assets/login.png
